<Window x:Class="WatchMe.Shelf.QuickLookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False" Topmost="True"
        AllowsTransparency="True" Background="Transparent"
        Width="540" Height="440" WindowStartupLocation="CenterScreen"
        PreviewKeyDown="OnPreviewKeyDown" Deactivated="OnDeactivated">
    <Window.Resources>
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Brush.ChipBg}" />
            <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Brush.HoverBg}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12" Background="{DynamicResource Brush.PanelBgSolid}"
            BorderBrush="{DynamicResource Brush.PanelBorder}" BorderThickness="1" Padding="14" Margin="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ContentControl x:Name="Host" Grid.Row="0" Margin="0,0,0,10" />

            <Border Grid.Row="1" Background="{DynamicResource Brush.AccentSoft}" CornerRadius="8" Padding="10,6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Style="{StaticResource NavButton}" Content="◀" Click="OnPrev" />
                    <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock x:Name="FileNameText" Foreground="{DynamicResource Brush.TextPrimary}"
                                   FontSize="12" TextTrimming="CharacterEllipsis" MaxWidth="300" HorizontalAlignment="Center" />
                        <TextBlock x:Name="MetaText" Foreground="{DynamicResource Brush.TextSecondary}"
                                   FontSize="10" HorizontalAlignment="Center" Margin="0,2,0,0" />
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Style="{StaticResource NavButton}" Content="▶" Click="OnNext" Margin="0,0,8,0" />
                        <Button Style="{StaticResource NavButton}" Content="✕" Click="OnClose" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
