<UserControl x:Class="WatchMe.Shelf.FileShelfView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AllowDrop="True" DragOver="OnDragOver" Drop="OnDrop"
             Focusable="False">
    <UserControl.Resources>
        <Style x:Key="ChipButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Brush.ChipBg}" />
            <Setter Property="Foreground" Value="{DynamicResource Brush.TextSecondary}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{DynamicResource Brush.ChipBorder}" />
            <Setter Property="Padding" Value="6" />
            <Setter Property="Margin" Value="0,0,8,4" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <StackPanel>
                                <Border Width="72" Height="54" CornerRadius="5"
                                        Background="#22FFFFFF" ClipToBounds="True">
                                    <Grid>
                                        <Image Source="{Binding Thumbnail}" Stretch="Uniform" Width="70" Height="52"
                                               RenderOptions.BitmapScalingMode="HighQuality" />
                                        <TextBlock Text="📄" FontSize="20" HorizontalAlignment="Center"
                                                   VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Thumbnail}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                </Border>
                                <TextBlock Text="{Binding Item.FileName}" FontSize="10" Width="74" Margin="0,3,0,0"
                                           Foreground="{DynamicResource Brush.TextPrimary}"
                                           TextTrimming="CharacterEllipsis" HorizontalAlignment="Center"
                                           ToolTip="{Binding Item.Path}" />
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Brush.HoverBg}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Accent}" />
                    <Setter Property="BorderThickness" Value="2" />
                    <Setter Property="Background" Value="{DynamicResource Brush.AccentSoft}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Item.Availability}" Value="Missing">
                    <Setter Property="Opacity" Value="0.45" />
                    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Danger}" />
                    <Setter Property="BorderThickness" Value="2" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Padding="8,2">
            <DockPanel>
                <TextBlock DockPanel.Dock="Right" Text="拖入文件 · 拖出取用 · 空格预览" FontSize="10"
                           Foreground="{DynamicResource Brush.TextSecondary}" VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📎 暂存架" FontSize="11" Foreground="{DynamicResource Brush.TextPrimary}"
                               VerticalAlignment="Center" />
                    <TextBlock x:Name="CountLabel" FontSize="10" Margin="6,0,0,0"
                               Foreground="{DynamicResource Brush.TextSecondary}" VerticalAlignment="Center" />
                </StackPanel>
            </DockPanel>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Auto" Padding="8,0,8,6"
                      MaxHeight="140">
            <ItemsControl x:Name="ChipsList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Vertical" MaxHeight="136" ItemHeight="100" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource ChipButton}" Focusable="True">
                            <Button.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="打开" Click="OnMenuOpen" />
                                    <MenuItem Header="在资源管理器中显示" Click="OnMenuReveal" />
                                    <Separator />
                                    <MenuItem Header="从暂存架移除 (Delete)" Click="OnMenuRemove" />
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
